{
  "name": "unpkg-app",
  "description": "The UNPKG web app",
  "private": true,
  "type": "module",
  "dependencies": {
    "chalk": "^5.4.1",
    "highlight.js": "^11.11.1",
    "mrmime": "^2.0.0",
    "preact": "^10.25.2",
    "preact-render-to-string": "^6.5.12",
    "pretty-bytes": "^6.1.1",
    "semver": "^7.6.3",
    "unpkg-tools": "workspace:^"
  },
  "devDependencies": {
    "@ryanto/esbuild-plugin-tailwind": "^0.0.1",
    "@types/bun": "^1.2.8",
    "@types/node": "^22.10.2",
    "@types/semver": "^7.5.8",
    "esbuild": "^0.24.2",
    "tailwindcss": "4.0.0-beta.9",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "pnpm run build:assets && pnpm run build:server",
    "build:assets": "node --disable-warning=ExperimentalWarning ../../scripts/build-assets.ts",
    "build:server": "tsc --project ./tsconfig.build.json",
    "dev": "MODE=development bun --port=3001 ./src/server.ts",
    "dev:assets": "bun ../../scripts/serve-assets.ts",
    "test": "bun --preload=./test/setup.ts test",
    "deploy": "cd ../.. && fly deploy . -c ./packages/unpkg-app/fly.json",
    "deploy:staging": "cd ../.. && fly deploy . -c ./packages/unpkg-app/fly.staging.json"
  },
  "files": [
    "assets-manifest.json",
    "dist",
    "public"
  ]
}
