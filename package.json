{
  "name": "unpkg-dev",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "git clean -fdX .",
    "pretest": "pnpm run build",
    "test": "pnpm -r test"
  }
}
